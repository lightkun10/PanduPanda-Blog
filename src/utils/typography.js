import Typography from 'typography';
import altonFontTheme from 'typography-theme-alton';

const typography = new Typography(altonFontTheme);
export default typography;
export const rhythm = typography.rhythm;